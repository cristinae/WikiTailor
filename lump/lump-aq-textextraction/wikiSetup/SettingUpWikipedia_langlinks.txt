1.Download the tables. Every sql file contains the CREATE TABLE command

#copied from the English tables
 CREATE TABLE `langlinks` (
  `ll_from` int( 8 ) unsigned NOT NULL DEFAULT '0',
  `ll_lang` varbinary( 20 ) NOT NULL DEFAULT '',
  `ll_title` varbinary( 255 ) NOT NULL DEFAULT '',
  UNIQUE KEY `ll_from` ( `ll_from` , `ll_lang` ) ,
  KEY `ll_lang` ( `ll_lang` , `ll_title` )
 ) ENGINE = InnoDB DEFAULT CHARSET = BINARY ;


2.All instances of the name of the table must be modified to fit the name in the database:

perl -pne 's/`langlinks`/`wikien_2015_langlinks`/' enwiki-20150112-langlinks.sql > enwiki-20150112-langlinks2.sql
 
 CREATE TABLE `wikiar_2015_langlinks` (
  `ll_from` int( 8 ) unsigned NOT NULL DEFAULT '0',
  `ll_lang` varbinary( 20 ) NOT NULL DEFAULT '',
  `ll_title` varbinary( 255 ) NOT NULL DEFAULT '',
  UNIQUE KEY `ll_from` ( `ll_from` , `ll_lang` ) ,
  KEY `ll_lang` ( `ll_lang` , `ll_title` )
 ) ENGINE = InnoDB DEFAULT CHARSET = BINARY ;
 
3.Upload:

mysql --user=lump  --host=mysql-rdlab.cs.upc.edu -p
use lump_pairs
source /media/dades1/cristina/wikipedia/2015/es/eswiki-20150124-langlinks2.sql
